# Write a script which displays only the login, UID and Path of each entry of the /etc/passwd file

cut -d: -f1,3,6 /etc/passwd

# cut -f1 returns just the first column
# -d delim - Use delim as the field delimiter character instead of the tab character.
